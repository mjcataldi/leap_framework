# Deprecated Compatibility Entry Point

This file is retained so older links and copied workflows do not break.

The active LEAP front door is now **LEAP Charter**, not Phase 0.

Use:

```text
templates/leap-charter-template.md
```

Current lifecycle:

```text
LEAP Charter -> LEAP Recon -> LEAP Prompt -> Implementation -> Validation/Handoff
```

Compatibility instruction:

```text
Run LEAP Charter using the current LEAP framework.
Use templates/leap-charter-template.md as the source of truth.
Treat any older "Phase 0" wording as legacy terminology for LEAP Charter.
```
